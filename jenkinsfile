pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                script {
                    docker.image('alpine').inside {
                        sh 'echo "Running inside Docker container"'
                    
                }
            }
            steps {
                sh '''
                    ls -la
                    node --version
                    npm --version
                    npm ci
                    npm run build
                    ls -la
               '''
            }
        }
    }
}
}
